# =============================================================================
# AI SEO Agent — Runtime Dependencies
# MVP Phase: Simple SEO Audit UI
# Target: Python 3.12+  |  OS: Windows
# Note: Python 3.14 is in use; versions are chosen for 3.14 wheel availability.
# =============================================================================

# --- Web Framework -----------------------------------------------------------

fastapi==0.115.6                  # ASGI web framework for API routes and static file serving
uvicorn[standard]==0.32.1         # ASGI server used to run the FastAPI application locally

# --- Data Validation ---------------------------------------------------------

pydantic==2.13.4                  # Request/response model validation and serialisation (requires pydantic-core >=2.44)
pydantic-settings==2.7.0          # Environment variable loading into typed settings models

# --- Configuration -----------------------------------------------------------

python-dotenv==1.0.1              # Loads variables from .env into the environment at startup

# --- HTTP Client and HTML Parsing --------------------------------------------

httpx==0.28.1                     # Async-capable HTTP client for fetching homepage, robots.txt, sitemaps
beautifulsoup4==4.12.3            # HTML parser for extracting title, meta, headings, links, alt text
lxml==6.1.1                       # Fast XML/HTML parser backend — 6.x series has Python 3.14 wheels

# --- AI / LLM ----------------------------------------------------------------

google-generativeai==0.8.3        # Google Gemini SDK — used for LLM-based SEO report generation

# --- PDF Generation ----------------------------------------------------------

reportlab==4.2.5                  # PDF generation library — converts Markdown report content to PDF
markdown==3.7                     # Parses Markdown to plain text/structure before PDF rendering

# --- Testing -----------------------------------------------------------------

pytest==8.3.4                     # Test runner for all unit and integration tests
pytest-asyncio==0.24.0            # Async test support for FastAPI and httpx coroutines
